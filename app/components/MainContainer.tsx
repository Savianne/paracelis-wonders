"use client"
import styled from "styled-components"

const MainContainer = styled.div`
    display: flex;
    flex: 0 1 100%;
    flex-wrap: wrap;
    overflow: hidden;
    
`

export default MainContainer;